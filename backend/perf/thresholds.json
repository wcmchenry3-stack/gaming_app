{
  "_comment": "SLOs for Render free-tier deployment. Warm-up must complete before timing starts. See docs/PERFORMANCE.md.",
  "scenarios": {
    "game_flow": {
      "description": "Full 13-round game (single user, sequential)",
      "users": 1,
      "p95_ms": 500,
      "error_rate_pct": 0.0,
      "endpoints": [
        "POST /yacht/new",
        "POST /yacht/roll",
        "GET /yacht/possible-scores",
        "POST /yacht/score",
        "GET /yacht/state (final)"
      ]
    },
    "leaderboard": {
      "description": "Concurrent leaderboard read/write (10 users)",
      "users": 10,
      "p95_ms": 300,
      "error_rate_pct": 1.0,
      "endpoints": ["POST /cascade/score", "GET /cascade/scores"]
    },
    "stateless_reads": {
      "description": "Read-only polling (20 users)",
      "users": 20,
      "p95_ms": 200,
      "error_rate_pct": 0.0,
      "endpoints": ["GET /yacht/state", "GET /yacht/possible-scores"]
    }
  }
}
