# OWASP ZAP Baseline Scan Configuration
# Tuned for the Gaming App JSON API (no HTML pages to crawl).
# Referenced by deploy.yml via called-deploy-render.yml zap-target-url parameter.
env:
  contexts:
    - name: "Gaming App API"
      urls:
        - "https://dev-games-api.buffingchi.com"
      includePaths:
        - "https://dev-games-api.buffingchi.com/.*"
      excludePaths:
        # OpenAPI docs are not a production attack surface
        - "https://dev-games-api.buffingchi.com/docs"
        - "https://dev-games-api.buffingchi.com/redoc"
        - "https://dev-games-api.buffingchi.com/openapi.json"
  parameters:
    failOnError: true
    failOnWarning: false
    progressToStdout: true

jobs:
  - type: passiveScan-wait
    parameters:
      maxDuration: 5

  - type: activeScan
    parameters:
      context: "Gaming App API"
      maxRuleDurationInMins: 5
      maxScanDurationInMins: 20
      addQueryParam: false
      defaultPolicy: "API Scan"
